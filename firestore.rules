rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules — tighten for production
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
